if(x<0){let sign="negative"};

x=x; 
if(x+x)
if(ifx)
if (x + x)
ifx (x + x)



class Test {
  return getx / gety;
  return x/y;
  
  let x = y / x;

  let x = y * x;

  let s = "Hello"/* test */;

  let x = 12345;
  if (x + y);
}

// Single comment
/* multiline comment
multiline comment
multiline comment
multiline comment */
